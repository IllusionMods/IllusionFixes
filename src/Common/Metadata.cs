namespace Common
{
    internal static class Metadata
    {
        /// <summary>
        /// Version number used by all plugins. Must be convertible to <see cref="System.Version"/>
        /// Uses ReleaseVersion.HotfixVersion format.
        /// </summary>
        public const string PluginsVersion = "1.7";
    }
}
